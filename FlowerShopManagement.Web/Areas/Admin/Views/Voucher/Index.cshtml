@model List<VoucherDetailModel>

<div class="row">
    <div class="col-lg-2 d-lg-block collapse borderCustom1 um">
        <div class="mt-3 col-lg-3" id="filter">
            <partial name="_Filter" view-data="ViewData" />
        </div>
    </div>


    <div class="col-lg-10 col-12 mt-3 mt-lg-0">
        <div class="d-flex justify-content-end d-lg-none">
            <button class="filter-icon border-0 fs-3" data-bs-toggle="collapse" data-bs-target=".um" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="h5">Filter</span>
            </button>
        </div>

        <partial name="_ViewAll" model=@Model />

    </div>

</div>

<button class="btn btn-primary icon-plus rounded position-fixed" style="right:0; bottom:60px;" onclick="location.href = '@(Url.Action("Create", "Voucher"))'">Create new Voucher</button>